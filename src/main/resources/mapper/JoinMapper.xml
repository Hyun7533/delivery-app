<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.delivery.deliveryback.join.JoinMapper">
    <insert id="addCustomer" >
        insert into dlvy.customer(id, pw, email, address, insert_time)
        values (#{id}, #{pw}, #{email}, #{address}, now())
    </insert>
</mapper>